### This file was generated by Nexus Schema
### Do not make changes to this file directly


type FollowerResult {
  error: String
  followers: [User]
  following: [User]
  ok: Boolean!
  totalPages: Int
}

type Mutation {
  createAccount(email: String!, firstName: String!, password: String!, username: String!): User
  editProfile(avatar: Upload, bio: String, email: String, firstName: String, password: String): OkResult
  followUser(username: String): OkResult
  signin(password: String!, username: String!): OkResult
  unfollowUser(username: String): OkResult
}

type OkResult {
  error: String
  ok: Boolean!
  token: String
}

type Query {
  seeFollower(page: Int!, username: String!): FollowerResult
  seeFollowing(cursor: Int, username: String!): FollowerResult
  seeProfile(username: String!): [User]
}

"""The `Upload` scalar type represents a file upload."""
scalar Upload

type User {
  avatar: String
  bio: String
  createdAt: String!
  email: String!
  firstName: String
  followers: [User]
  following: [User]
  id: String!
  password: String!
  totalFollower: Int!
  totalFollowing: Int!
  updatedAt: String!
  username: String!
}